name: Minimal Test

on:
  workflow_dispatch:
  push:
    branches: [ master, main ]

jobs:
  echo-test:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello from minimal test"
      - name: List actions uses
        run: |
          grep -R "uses:\s" .github/workflows || true
